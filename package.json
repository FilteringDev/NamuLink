{
  "name": "@filteringdev/namulink",
  "version": "0.0.0",
  "description": "",
  "type": "module",
  "scripts": {
    "build:stable": "npm run build -w builder -- --minify true --use-cache false --build-type production --SubscriptionUrl https://cdn.jsdelivr.net/npm/@filteringdev/namulink@latest/dist/NamuLink.user.js",
    "build:dev": "npm run build -w builder -- --minify false --use-cache false --build-type production --SubscriptionUrl https://cdn.jsdelivr.net/npm/@filteringdev/namulink@latest/dist/NamuLink.user.js",
    "debug": "npm run debug -w builder",
    "lint": "pnpm --filter @filteringdev/namulink-builder run lint && pnpm --filter @filteringdev/namulink-userscript run lint",
    "test": "npm run test -w testunit"
  },
  "keywords": [
    "namu.wiki",
    "NamuWiki"
  ],
  "files": [
    "dist/**/*"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/FilteringDev/NamuLink.git"
  },
  "license": "MPL-2.0",
  "workspaces": [
    "userscript",
    "builder",
    "testunit"
  ],
  "devDependencies": {
    "oxlint": "^1.79.0",
    "oxlint-tsgolint": "^7.0.2001"
  }
}
